import 'package:flutter/material.dart';
import '../constant.dart';

class ProductListPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Center(child: Constant().text16("All Products"));
  }

}